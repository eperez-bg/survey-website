/// Temporary UI payload used while dragging a canopy corner onto the map.
///
/// This is not survey data and is never serialized. It only lets the canvas
/// distinguish a canopy-corner drag from a table drag.
class CanopyCornerDragData {
  const CanopyCornerDragData();
}
